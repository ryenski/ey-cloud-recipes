check process redis-stonith
with pidfile <%= @pidfile %>
  start program = "/usr/bin/redis-server <%= @configfile %>"
  stop program = "/usr/bin/redis-cli -p <%= @port %> shutdown"
  group redis-<%= @name %>
